# Server Configuration
PORT=8080
ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-database-password
DB_NAME=clean_architecture_api

# JWT Configuration
JWT_SECRET_KEY=your-jwt-secret-key

# Logging
LOG_LEVEL=debug

# GitHub SHA for Docker image tagging
GITHUB_SHA=latest
