version: "2"

run:
  timeout: 5m
  go: "1.24"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - funlen
    - nestif
  disable:
    - revive

formatters:
  enable:
    - gofmt
    - goimports
    - gofumpt
